# Example environment variables for ClearEye work-app

# Google OAuth (used by @auth/sveltekit)
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=

# AWS / DynamoDB (used by src/lib/server/db.ts)
# Either set AWS_PROFILE to use local AWS credentials, or provide keys
AWS_PROFILE=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1

# AI provider selection: 'gemini' | 'openai' | 'anthropic'
AI_PROVIDER=gemini

# API keys for AI providers (only set the one(s) you use)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=
